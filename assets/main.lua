function Init(dt)
	print("[LUA] "..dt.." initializing...")
end

function Update(dt)
	print("[LUA] updating...")
	-- Should execute registered c++ calls here!
end

-- Possibly LateUpdate and FixedUpdate?

function Draw(dt)
	print("[LUA] drawing...")
end